import { StorageType, SessionRecordType } from '../types'
import { SignalProtocolAddress } from '../signal-protocol-address'
declare class SessionRecord {
    static deserialize(serialized: SessionRecordType): SessionRecord
    serialize(): SessionRecordType
}
export interface MessageType {
    type: number
    body?: string
    registrationId: number
}
export declare class SessionCipher {
    storage: StorageType
    remoteAddress: SignalProtocolAddress
    constructor(storage: StorageType, remoteAddress: SignalProtocolAddress)
    getRecord(encodedNumber: string): Promise<SessionRecord | undefined>
    private encryptJob
    private loadKeysAndRecord
    encrypt(buffer: ArrayBuffer, encoding: string): Promise<MessageType>
}
export {}
